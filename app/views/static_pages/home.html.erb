<%= render 'shared/banner' %>

<% @physical_books.each_slice(3) do |books| %>
  <div class="row">
    <% books.each do |book| %>
      <div class="col-md-4">
        <div class="row">
        <div class="card" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.2);">
        <div class="col-xs-4">
            <div class="card-cover"><%= image_tag "#{book.cover_pic_url}" %></div>
        </div>
        <div class="col-xs-8 cont">
           <div class="card-category"><%=  book.status %></div>
            <div class="card-description">
              <p><%= book.title %></p>
              <p><%= book.author %></p>
            <img class="card-user avatar" src="#{book.user_id.profile_pic_url}">
            <a class="card-link" href="#" ></a>
            </div>
            </div>
          </div>
        </div>
      </div>
    <% end %>
  </div>
<% end %>


